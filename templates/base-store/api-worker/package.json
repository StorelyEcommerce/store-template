{
    "name": "edge-api",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "dev": "wrangler dev --port 8787",
        "deploy": "wrangler deploy",
        "build": "tsc"
    },
    "dependencies": {
        "hono": "^3.0.0",
        "stripe": "^14.0.0"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.0.0",
        "wrangler": "^3.0.0"
    }
}
